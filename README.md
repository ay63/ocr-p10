# BobApp

Clone project:

> git clone XXXXX

### Docker compose
Go to the root of the project where you can find the docker-compose.yml file

Build container: 

> docker-compose build

Run the container:

> docker-compose up -d
